# UI Suite Monthly #33 Demo Page

A bold, responsive demo page for the UI Suite Monthly issue #33 exploration.

## Run locally

Open `index.html` directly in a browser or serve the folder with any static server.

## Scripts

- `npm run lint`
- `npm test`
